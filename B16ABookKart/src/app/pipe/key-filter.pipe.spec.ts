import { KeyFilterPipe } from './key-filter.pipe';

describe('KeyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
